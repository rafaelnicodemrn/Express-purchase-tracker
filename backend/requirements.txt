# Backend — Express Purchase Tracker
# Gerado para Python 3.11+

# Runtime
fastapi==0.115.14
uvicorn[standard]==0.35.0
pydantic==2.11.7

# Testes e qualidade de código
pytest==8.4.0
pytest-cov==6.1.0
httpx==0.28.1          # cliente async para TestClient do FastAPI
ruff==0.11.13          # lint + formatter (substitui flake8 + black + isort)
